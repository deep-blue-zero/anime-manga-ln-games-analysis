# Character registry

`registry.jsonl` is the curated source for `../CHARACTER_ANALYSIS_INDEX.md`. The Markdown index is generated deterministically and must not be edited independently.

The registry is intentionally empty during G3. Candidate discovery and owner-reviewed population begin in later migration phases.
