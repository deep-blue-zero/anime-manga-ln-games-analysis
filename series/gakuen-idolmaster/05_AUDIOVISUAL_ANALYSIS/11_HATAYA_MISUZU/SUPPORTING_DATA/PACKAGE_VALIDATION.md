---
series: GKM
generation: V2
artifact_type: audiovisual_documentation
status: canonical
supersedes: []
superseded_by: []
do_not_use_as_current_authority: false
source_boundary: "AVE-FULL-20260910: 27 source recordings computationally processed; 565 sampled still points reviewed; no fresh direct listening or continuous-motion review. Earlier evidence generations retain their stated provenance."
last_updated: '2026-09-10'
---

> **Public repository representation.** Machine-specific paths use `WORKSPACE/`, `LOCAL_USER/` or `LOCAL_DRIVE_*` placeholders; configure these roots before reproduction. Direct Drive URLs are represented by source-object IDs. Original execution hashes and exported-byte claims identify the archived originals; public code/data snippets containing these substitutions are explicitly derivatives. The import manifest records their final repository hashes and decoded payload hashes.


> **Repository adoption — 2026-09-10.** This is the owner-requested branch revision of the exported rebuild. Pre-import candidate labels and repository-state statements in preserved records describe their original execution. Current analytical use remains bounded by the stated evidence modalities. The [import record](../../../10_RELEASE_MANIFEST_AND_ARCHIVE/GKM_AVE_FULL_REBUILD_IMPORT_20260910.md) identifies export hashes and repository transformations.

# AVE-FULL-20260910 — independent candidate validation

**Current candidate content: PASS. Fresh perceptual coverage remains partial.** This report covers the full toolkit generation and supersedes the historical validation retained below. It validates source and document integrity, computational provenance, arithmetic, point-accounting and claim traceability; it does not certify complete watching/listening or the truth of every interpretation.

Snapshot: `2026-09-10T21:59:14.315810+00:00`. Repository HEAD is `400234a42d5811847367de94e6e8229fe816e99b` on `series/gakuen-idolmaster`, with a clean working tree rechecked read-only. Cached `origin/main` is `2c07ff0733686aa9509e7ab0ac52292a4f4f77e0`; it differs and was not fetched. No new remote baseline, canonical integration or modification of earlier releases is claimed.

## Scope and disposition

| Item | Current result |
|---|---|
| Candidate topology | 39 Markdown files; all 25 members of this character's prior delivered packet retained. Across both packets, all 52 previous Markdown members remain. |
| Original sources | All 27 original logical source IDs, their order and every original manifest field retained; current materializations freshly hash/stream checked. |
| Whole-source calculations | 27 native selected-stream mixed-audio loudness records and 27 source-bound per-channel feature records; no silent mean-channel voice substitute. |
| Fresh still review | 565 distinct original-source PTS points in 57 actual image-display events, across all 27 sources. These are sampled points, not watched seconds. |
| Fresh audio and motion perception | Zero declared listening seconds and zero continuous-motion review seconds. Each modality retains the full scoped source-interval requirement (18963.117301587 seconds summed across sources), rather than substituting stills or computation. |
| Original main claims | All 45 original IDs/propositions and historical matrix rows retained, with explicit previous/current dispositions. |
| Current dispositions | 8 SUPPORTED / 27 QUALIFIED / 4 REVISED / 6 UNRESOLVED. SUPPORTED can support an original rejection/downgrade; it does not automatically endorse the proposition being rejected. |
| Portable supporting data | Current tables agree with the final structured records. Source/stream/original-PTS/frame/contact/receipt joins and frozen text locators pass. |
| Package form | Markdown only. Local Markdown destinations and heading anchors resolve within this individual character directory. Media, raw receipts and runtime artifacts remain in the separate evidence workspace. |

MZ-AV-019/023/025 change QUALIFIED to REVISED: withdraw an untested VEIL superlative, replace invariant ensemble centrality with local distributed focality, and separate varied body poses from static-art source form. MZ-AV-042 retains its prior source correction.

The separate canonical-core companion retains all 18 MZ-R identifiers, original premises and transition labels, an explicit current crosswalk to the 45 matrix claims, and the complete historical repository addendum. These are eighteen integration rows, not eighteen extra matrix claims. The current text companion identifies 18 freshly acquired CIDOL scripts and 10 previously acquired Dear scripts whose local bytes were newly rechecked.

MZ-AV-010 no longer uses M02 supplemental F000003 at 781 seconds as positive Rinha evidence. The image belongs to the earlier Temari/pumpkin exchange. The frozen actual response, authored note and source point remain in the observation record; removing this claim selection changes neither the 565-point coverage nor its provenance. Positive friendship/training selections use the appropriate later points, including the 990-second friend caption. Nonrestoration remains a separate locked-text/inherited argument. The corrected export has 333 selected fresh references.

## Computational and presentation checks

The complete corpus snapshot contains **359 completed computation, extraction and initial-admission runs**: 60 whole-source loudness, 64 feature runs (60 whole-source plus four chapter compilations), four chapter-region batches, 12 audio comparisons, two native audio clips, 108 frame runs, 108 contact-sheet runs and one initial inventory. The final actual-PTS inventory and formal review validation are two separate accounting runs; 359 is not a count of every final run directory.

The independent generated-data audit passes 19,743 checks. It binds parameters and dependencies to sources, verifies absolute stream indices and original-source clocks, checks per-channel feature/sample identities and interval bounds, verifies current frame PTS membership, and distinguishes successful runs from failed diagnostics. The separate input/reduction audit passes 9,529 checks, including arithmetic from all 1,201 saved PNGs and the declared equal-recording group statistics. Those image descriptors describe encoded pixels; their adjacent-sample differences are not cuts or motion. Group dB/LU means are equal-recording summaries, not pooled energy or significance tests.

All 37 operational Misuzu chapter windows additionally have fresh selected mixed-audio loudness and source-bound per-channel features. Their 20 label-cycle and 17 embedded-marker boundaries remain inherited operational definitions. One independently invoked public Dear001 measurement exactly reproduces the batch selected PCM, intervals, filter values and raw outputs in ten checks. This is a public-API reproduction of the same algorithm, not an independent DSP implementation or speech alignment.

Eleven whole-recording same-title/cross-character comparisons are DIFFERENT as native decoded audio objects. That is not a finding of different acting quality or perceptual dissimilarity. The bounded H04 original/trim comparison at source seconds 3160–3205 is MATCH after verified native WAV extraction; it does not establish full-file or audiovisual equivalence. Previous retained-packet-prefix findings remain historical evidence. Layout, timing, positive sample counts and ordered sample equality remain separate predicates in [audio identity](FULL_REBUILD_AUDIO_IDENTITY.md).

The isolated copied toolkit passes **98 synthetic regression tests** (runner 53.497 seconds; exit 0), with tested code unchanged. This uses the previously tested WAV-layout patch while preserving native ordered samples, not an unmodified original release. Runtime, exact commands, canonical-source/decoded-sample timing, gap/clip rules and reproduction source are in [methods](FULL_REBUILD_METHODS_AND_REPRODUCTION.md). Fenced source reconstruction and numerical Markdown transcription pass 1,747 checks; the final documentation generator is explicitly distinguished from the earlier numerical-render snapshot. No embedded document instructions were executed by this audit.

Formal accounting uses a fresh public inventory with 60 logical source/materialization rows and actual selected PTS requirements. The raw-response audit passes **6,281 checks over 118 actual saved tool responses** across both characters: returned PNG bytes match their presented sheet artifacts, and sheet membership maps to exact source points. All 1,201 required still points are declared with zero missing points. The public validator reports `structural_valid: true`, `PARTIAL_DECLARED_COVERAGE`, and `perception_truth: NOT_ESTABLISHED_BY_VALIDATION`. Authored notes, including interpretation, remain separate from returned-image receipts. Additional overlapping semantic audit views and prior-trial points receive no new coverage credit. See [review accounting](FULL_REBUILD_REVIEW_ACCOUNTING.md).

## Resolved discrepancies and remaining limits

The H24 input-seek failure was recovered using a fresh source-origin decode with original assertions active. Failed outputs remain diagnostic and were not admitted or counted. The initial execution-adapter inventory's extra dependency row was not forced through the public review validator; the final public inventory uses the correct media-row schema. A reproduction-source CRLF/LF discrepancy was corrected and the final fenced bytes rechecked. Current table wording matches structured exports; seven Misuzu presentation rows differ only by added spaces before numbers, without changing words, values or dispositions.

Source-wide loudness and features do not isolate the actor or establish softness, breath, hesitation, intention, genre, mastery or causality. No fresh F0, tempo, VAD, speech alignment, direct listening or continuous choreography review was completed. Operational chapter clocks are not speech timestamps; caption visibility and prior-trial caption navigation remain distinct from heard timing. Historical acoustic/continuous-performance findings are preserved as inherited work, not relabeled as freshly verified. Ethical and relational questions remain open where the individual claims say so.

The final current-table/companion audit passes 370 checks; claim traceability passes 1,380 Hiro and 5,832 Misuzu checks; the final pre-seal topology/field/link snapshot passes 1,778 checks with no errors or warnings. These are overlapping validation predicates, not independent observations or an additive evidential score.

## Seal and audit records

This report freezes before [the checksum inventory](../CHECKSUMS.md) is regenerated. That inventory covers every other Markdown file and excludes itself. Archive member/CRC/byte equality and final archive hashes are checked after freezing, in the external delivery record; no ZIP result is predeclared here. Earlier character ZIPs, the original toolkit release and source videos remain unchanged. The checksum and archive checks certify bytes, not perceptual completeness.

The following records remain in the companion evidence workspace at `WORKSPACE/Gakuen Idolmaster/work/ave-full-rebuild-20260910`. Their paths and hashes identify the completed checks, without making the Markdown packet depend on a cross-packet link.

```json
[
  {
    "path": "audit/isolated-regression-status.json",
    "status": "PASS",
    "checks_or_tests": 98,
    "sha256": "e3a4ef0b5f521b9963fb7a36bd9515710f4078fbed31456c91d89f38fab99684"
  },
  {
    "path": "audit/generated-corpus-complete.json",
    "status": "PASS",
    "checks_or_tests": 19743,
    "sha256": "41f30b1141a445d9b46857eccc6505654de3031afe8ba28c1afc55cea968e304"
  },
  {
    "path": "audit/inputs-reductions-pixels.json",
    "status": "PASS",
    "checks_or_tests": 9529,
    "sha256": "4114f124067d43e14af3e4d219ae29390326c6cc50fc00d6022191587c01d89b"
  },
  {
    "path": "audit/technical-markdown-final.json",
    "status": "PASS",
    "checks_or_tests": 1747,
    "sha256": "96db1679def0f18f78318d2e2c26ce7b117f1c701944d9d29df87c285a9283ad"
  },
  {
    "path": "audit/public-region-reproduction.json",
    "status": "PASS",
    "checks_or_tests": 10,
    "sha256": "69f1fcfb3092c1413d68d800203cc19c2d4daa7e01c789d6b2a73860ac3c6d1b"
  },
  {
    "path": "evidence/review-accounting/raw-receipt-audit.json",
    "status": "PASS",
    "checks_or_tests": 6281,
    "sha256": "6f4051e5418837aa8fea8f5223aac8e0f333dc337f0b4481fae411a22b94838c"
  },
  {
    "path": "audit/hiro-claims-final.json",
    "status": "PASS",
    "checks_or_tests": 1380,
    "sha256": "9954cdad046511685a6a22ce2b920b2e85d2e87bfcba0f423c5829742022549d"
  },
  {
    "path": "audit/misuzu-claims-final.json",
    "status": "PASS",
    "checks_or_tests": 5832,
    "sha256": "0eefb20478f7e4d6569c9ca997cea7919ac062db5f4c5e673d0b85d5c1f645dd"
  },
  {
    "path": "audit/final-companions-confirmed.json",
    "status": "PASS",
    "checks_or_tests": 370,
    "sha256": "b54bdc73400199fe6f59057d7936a64ede19d4e36b7a199ec880614627953b71"
  },
  {
    "path": "audit/documents-final-content.json",
    "status": "PASS",
    "checks_or_tests": 1778,
    "sha256": "0c6fe35686a2dca702f884d5dc7563f21bb30f8010941882ac4e6c81d1fff02b"
  }
]
```

---

## Complete historical validation from the previous delivered packet

Everything below is retained historical validation from the previous targeted execution. Its counts, software comparisons, completed checks and uses of “current” refer to that prior generation, not AVE-FULL-20260910. Prior document SHA-256: `d3e5af2a312613210a8f8114b4dfd2256b45fcd8a6f5ddaca4d86bf6b91873f5`. The current report above governs this candidate.

# MISUZU final candidate validation and discrepancy audit

**Candidate document, source-accounting and numerical checks pass.** This is an authorized-execution integration candidate, not a new canonical release. The technical/documentation repair is substantially complete under the stated methods. Renewed audiovisual interpretation remains bounded: no new direct listening was possible, all freeform audio-model trials were rejected, and ethical/relational OPEN questions are not declared resolved. A filled table alone does not certify performance analysis.

Fixed repository baseline: `400234a42d5811847367de94e6e8229fe816e99b`, branch `series/gakuen-idolmaster`. The repository remained unchanged and clean at finalization. Original releases, their checksum records and both supplied ZIPs remain untouched. Complete maintained-document changes against both the pinned repository and supplied ZIP generation are in [MAINTAINED_DOCUMENT_DIFFS.md](MAINTAINED_DOCUMENT_DIFFS.md).

## Scope actually validated

| Check / evidence class | Validated coverage |
|---|---|
| Final candidate files | 25 Markdown documents; checksum inventory covers the other 24 documents and explicitly excludes CHECKSUMS.md itself. |
| Original source fields | All 27 original source IDs/order, 324 original source leaf fields and 16 root fields are preserved in the complete rebuilt manifest. |
| Current source receipts | All 27/27 recorded source slots locally recovered, hashed/probed and matched to the exact recorded identities with explicit object/derivative limits. Duplicate local filename variants are accepted only with the same source ID and byte hash. |
| Inherited numerical data | 27 complete whole-source records; source/probe/summary agreement, sample/STFT counts, RMS ratios, silence sums and sparse visual reductions checked independently from the supplied tables. These checks do not relabel the prior run as current execution. |
| Current numerical data | 21 result records; 1892 scheduled and successful image samples, zero omitted reads. Actual input hash, cache fingerprint, sample counts, interval bounds, loudness/silence reductions and visual reductions passed. |
| Current Markdown rendering | Every current result’s metadata, full probe, runtime/parameters, audio features, loudness, silence intervals and sampled visual rows matches its working result at the declared display precision. Embedded helper bytes match the executed script SHA-256 after the specified LF/no-BOM extraction. |
| Claim inventory | All 45 original IDs retained; synchronized current review tables contain 8 SUPPORTED, 30 QUALIFIED, 1 REVISED, 6 UNRESOLVED. Review disposition describes the bounded renewed evidence, not a global re-certification of the earlier analysis. |
| Locked A1 receipts | 37 newly acquired files checked against actual byte sizes, SHA-256, computed Git blob SHA-1 and the exact frozen upstream tree receipt. Source-lock commit remains 00d150a069a3ffa723a1ff264752ba242024caad. |
| ASR and rejected audio models | Nine ASR clips independently decoded again: all PCM hashes and sample counts match. Segment tables, source/result hashes and embedded ASR script hash match. Five corrected trial clips per freeform model remain rejected; none promotes a qualitative claim. |
| Local links and code syntax | Package-local file links/headings resolved; all fenced Python blocks parse. External source URLs are preserved; this local-link audit does not claim every external page was re-fetched. |

All **37 chapter-associated operational intervals** are present in four identical summary tables. Dear011–027 retain 17 inherited container-marker measurements; Dear001–010 and028–037 use 20 newly reviewed label-cycle intervals. Every new boundary’s frame index/time agrees with the validated interval specification and every new summary value agrees with the independently generated current result. The inherited late interior-window measurements remain identifiable as a different observation.

Fresh visual entries carry source IDs/hashes, exact timestamp schedules or boundary-frame references, view mode and claim consequences. Adjacent-label inspection establishes a reproducible compilation convention; it does not make a crossfade’s original-game semantic onset exact to 1/30 second. The auditor verifies locator/accounting consistency without claiming to have directly viewed all the character reviewer’s frames.

## Substantive discrepancies resolved

The supplied ZIPs’ acquisition, computation and inspection statements are explicitly an inherited generation even though their date matches this execution. Current receipts, calculations, direct still-image review and automated audio candidates have separately named records. All required media is now local; no whole-source acquisition request remains. The optional historical records and interpretive dependencies in [PENDING_DEPENDENCIES_AND_REQUESTS.md](PENDING_DEPENDENCIES_AND_REQUESTS.md) are not silently turned into mandatory new media scope.

The current M03 reproduction check uses the same input hash as the incoming record. Audio sample counts, spectral/RMS descriptors and EBU R128 summaries agree within the declared comparison tolerance. Corresponding low-level interval endpoints differ by no more than0.005 seconds, and their differently rounded endpoints produce a small difference in the summed duration. OpenCV pixel-derived summaries also differ despite matching requested sample times and corresponding decoder times. The cause of the pixel differences was not isolated, so they cannot establish fine acting, choreography or shot-count differences. Music-tracker features were not generated for this M03 check. See [EXECUTION_MEASUREMENTS.md](EXECUTION_MEASUREMENTS.md).

The qualitative inventory is45 original rows:43 claims plus the two original ethical/relational OPEN rows, MZ-AV-044/045. All are retained. Six empty table instances across four maintained documents were repaired; the prompt’s shorthand count was not used to omit the specialist documents. MZ-AV-042 distinguishes present verified bytes from conflicting historical acquisition accounts, which cannot be reconciled by retroactively dating this upload.

Dear001 P10=0 produces a floor-dependent RMS ratio, not isolated spoken dynamic range. M03’s2835.0-second container outlasts its2834.529524-second first audio stream; current sample duration follows the audio stream without artificial padding. New late chapter-associated intervals include the previously omitted final-yawn area, but the visual caption and an ASR omission do not establish a heard shared yawn. Optional solos remain outside this rebuild.

## Retained warnings and limits

The mechanical audit reports differences in original claim rows only where an explicit correction was required: MZ-AV-042 corrects contradictory source-acquisition provenance. Those are reviewed revisions, not unreported deletions; all other original qualitative rows remain preserved. Full diffs identify changes outside the table as well.

The supplied-generation source processor remains preserved as historical code. Its old cache path can return before hashing the input. These are retained historical-code audit warnings, not an approved current reproduction route. The current helper in [EXECUTION_MEASUREMENTS.md](EXECUTION_MEASUREMENTS.md) uses explicit inputs and hashes before cache acceptance and supplies runnable specifications without the missing-probe dependency.

No new direct auditory observation is claimed. ASR supplies fallible passage candidates, while both freeform audio models fail source-control checks and are rejected in [AUDIO_MODEL_REVIEW.md](AUDIO_MODEL_REVIEW.md). Historical listening claims are not declared nonexistent; they remain historical. Mixed audio and whole-frame metrics do not identify an individual voice, breathiness, emotion, pose quality or narrative cause. Direct visual coverage is limited to the schedules in [the character execution review](EXECUTION_EXTENSION_20260910.md).

## Package integrity and independent recheck

The final candidate is Markdown-only, with one character directory and a complete SHA-256 inventory. Archive CRC, member inventory and byte-for-byte correspondence to this finalized tree are checked separately after archive creation; the archive SHA-256 is reported with delivery rather than inserted into the archive itself. This separates archive integrity from interpretive validity.

Save the following Python code outside the package and supply the delivered ZIP path to independently check CRC, path safety, one character root, Markdown-only membership, duplicate members and every document checksum. It reads the archive and writes no files.

```python
import argparse, hashlib, re, zipfile
from pathlib import PurePosixPath, Path
parser = argparse.ArgumentParser()
parser.add_argument('archive')
args = parser.parse_args()
archive = Path(args.archive)
with zipfile.ZipFile(archive) as z:
    assert z.testzip() is None, 'ZIP CRC failure'
    names = [i.filename for i in z.infolist() if not i.is_dir()]
    assert len(names) == len(set(names)), 'Duplicate ZIP members'
    assert all(PurePosixPath(n).suffix.lower() == '.md' for n in names)
    assert all(not PurePosixPath(n).is_absolute() and '..' not in PurePosixPath(n).parts for n in names)
    roots = {PurePosixPath(n).parts[0] for n in names}
    assert len(roots) == 1
    root = next(iter(roots)) + '/'
    assert root[:-1] in {'07_SHINOSAWA_HIRO', '11_HATAYA_MISUZU'}
    checksum_name = root + 'CHECKSUMS.md'
    text = z.read(checksum_name).decode('utf-8-sig')
    rows = [line.strip().strip('|').split('|') for line in text.splitlines() if line.startswith('|')]
    inventory = {r[0].strip(): r[1].strip() for r in rows if len(r) == 2 and re.fullmatch('[a-f0-9]{64}', r[1].strip())}
    assert set(inventory) == {n[len(root):] for n in names if n != checksum_name}
    for name, expected in inventory.items():
        assert hashlib.sha256(z.read(root + name)).hexdigest() == expected, name
print('PASS: CRC, one character root, Markdown-only, unique members, complete SHA-256 inventory')
print('ZIP SHA-256:', hashlib.sha256(archive.read_bytes()).hexdigest())
```

